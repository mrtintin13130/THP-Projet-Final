<% category = @category %>
<% article = @article %>
<% user = @user %>
<h1>Article</h1>
Propirétaire: <%= user.find(article.user_id).first_name %> <%= user.find(article.user_id).last_name %>
<br>
Description: <%= article.description %>
<br>
Taille: <%= article.size %>
<br>
Catégorie: <%= category.find(article.category_id).name %>
